# 环境变量配置示例
# 复制此文件为 .env 并填入实际值（.env 已在 .gitignore 中，不会被提交）

# ============ GLM API 配置 ============

# GLM API Key（必需）
# 从 https://open.bigmodel.cn/ 获取
GLM_API_KEY=your-glm-api-key-here

# GLM 模型选择（可选）
# 可选值：glm-4-flash, glm-4-air, glm-4-airx, glm-4, glm-4-plus
GLM_MODEL=glm-4-flash

# ============ 前端 AI 对话配置 ============

# ⚠️ WARNING: 以下配置会注入前端构建产物，任何人都可查看，请勿使用生产 API Key

# 前端直连 API Key（可选，构建时注入，前端可见，仅适合测试）
AI_API_KEY=your-ai-api-key-here

# AI 接口地址（可选）
AI_API_ENDPOINT=https://open.bigmodel.cn/api/paas/v4/chat/completions

# 模型名称（可选）
AI_MODEL=glm-4-flash

# 前端访问密码（可选，仍会暴露在前端）
AI_PASSWORD=your-password-here

# 密码缓存位置（可选）：session 或 local
AI_AUTH_STORAGE=session

# ============ GitHub Actions 配置 ============

# 在 GitHub 仓库设置中添加以下 Secrets：
# 
# 1. GLM_API_KEY
#    - 名称：GLM_API_KEY
#    - 值：你的 GLM API Key
#    - 用途：AI 文档生成工作流
#
# 2. ACONGM_VUEPRESS（已有）
#    - 名称：ACONGM_VUEPRESS
#    - 值：GitHub Personal Access Token
#    - 用途：自动部署到 GitHub Pages

# ============ 本地开发配置 ============

# 时区设置
TZ=Asia/Shanghai

# Node.js 版本
NODE_VERSION=20

# ============ 安全提示 ============

# 1. 永远不要提交 .env 文件到 Git
# 2. 定期轮换 API Key（建议每 90 天）
# 3. 不同环境使用不同的密钥
# 4. 监控 API 使用情况和成本
# 5. 如果密钥泄露，立即撤销并生成新密钥

# ============ 使用方法 ============

# 方式 1：直接使用环境变量
# export GLM_API_KEY="your-key"
# node lib/glm-api.mjs generate "主题"

# 方式 2：使用 dotenv（需安装 dotenv 包）
# npm install dotenv
# node -r dotenv/config lib/glm-api.mjs generate "主题"

# 方式 3：在脚本中加载
# import 'dotenv/config'
# // 然后调用 API

# ============ 验证配置 ============

# 检查环境变量是否设置
# echo $GLM_API_KEY | head -c 10

# 测试 API 调用
# node lib/glm-api.mjs chat "你好" --model glm-4-flash
